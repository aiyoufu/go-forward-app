#!/bin/sh
# 推送前门禁：本地冒烟全套（启用：git config core.hooksPath .githooks）
cd "$(git rev-parse --show-toplevel)" || exit 1

echo "[pre-push] node scripts/run-all-smoke.mjs"
node scripts/run-all-smoke.mjs || {
  echo "[pre-push] BLOCKED: smoke suite failed" >&2
  exit 1
}

echo "[pre-push] all gates passed"
